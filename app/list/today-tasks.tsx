import TaskScreenHeader from '@/components/TaskScreenHeader';

const TodayTasksScreen = () => {
  return <TaskScreenHeader title="Bugünün Görevleri" showDate={true} />;
};

export default TodayTasksScreen;
